# Sprint 01 — Flutter Scaffold

**Goal:** Working Flutter app launching a placeholder home screen on
both iOS and Android with the correct architecture, dependencies, and
lint rules.

## Tasks

1. Install Flutter SDK
2. Run `flutter create` at repo root with org com.videoconverterpro
3. Configure pubspec.yaml with locked tech stack
4. Set up feature-first folder structure under lib/
5. Add build_runner + freezed + riverpod_generator + json_serializable
6. Configure analysis_options.yaml with strict flutter_lints
7. Create core/theme with Material 3 theme (primary #2563EB, dark mode)
8. Create shared logger service (never print())
9. Create placeholder HomePage showing "Video Converter Pro"
10. Set up go_router with a single route
11. Verify flutter analyze passes with zero warnings
12. Verify flutter run works on iOS simulator or Android emulator
13. Update context/CURRENT_STATE.md and context/NEXT_STEPS.md
14. Commit: chore(scaffold): initial Flutter project with Riverpod and clean architecture

## Definition of Done

- App launches on at least one platform
- Folder structure matches copilot-instructions
- Zero analyzer warnings
- context/ files updated
- Commit pushed
