# Next Steps

1. Commit platform folder regeneration (android/ios/web/etc + .metadata)
2. Install Android SDK when ready, then do a real device/emulator smoke
   test of the conversion pipeline (pick a real video, confirm it converts,
   confirm progress + before/after size display correctly)
3. Figure out iOS build verification path (no Mac on hand — likely a CI
   runner later)
4. Push to origin/work
5. Begin Sprint 3 — Presets UI (compression presets, destination presets,
   Conversion Setup / Progress / Results screens)
