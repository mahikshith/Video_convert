# Next Steps

1. Verify flutter analyze passes (run `flutter analyze` with mirror env vars)
2. If dart:ui errors persist, run `flutter pub get` again (it re-downloads sky_engine)
3. Fix any analyzer warnings in lib/ code
4. Commit Sprint 1: `chore(scaffold): initial Flutter project with Riverpod and clean architecture`
5. Push to origin/work
6. Begin Sprint 2 — FFmpeg Engine
