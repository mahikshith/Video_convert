# Next Steps

1. Commit and push Sprint 5 (History & Settings)
2. Install Android SDK when ready, then do a real device/emulator smoke
   test covering everything built so far, including Hive persistence
   across app restarts
3. Figure out iOS build verification path (no Mac on hand — likely a CI
   runner later)
4. Begin Sprint 6 — Paywall. This will need real RevenueCat product/API
   credentials from the user before it can be meaningfully implemented and
   tested beyond scaffolding — flag this when starting that sprint rather
   than assuming placeholder credentials are fine to ship.
