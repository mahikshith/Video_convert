# Next Steps

1. Commit Sprint 2 (FFmpeg engine implementation)
2. Run `flutter create .` to regenerate `android/`/`ios/` platform folders
   (currently missing — git-ignored, wiped by the Windows reset)
3. Install Android SDK when ready, then do a real device/emulator smoke
   test of the conversion pipeline (pick a real video, confirm it converts,
   confirm progress + before/after size display correctly)
4. Figure out iOS build verification path (no Mac on hand — likely a CI
   runner later)
5. Push to origin/work
6. Begin Sprint 3 — Presets UI (compression presets, destination presets,
   Conversion Setup / Progress / Results screens)
