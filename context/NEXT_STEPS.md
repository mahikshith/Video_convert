# Next Steps

1. Commit and push Sprint 4 (GIF + Audio)
2. Install Android SDK when ready, then do a real device/emulator smoke
   test covering everything built so far: video convert + all compression/
   destination presets, GIF (loop, fps, trim), audio (MP3/AAC/WAV), Cancel,
   Save, Share
3. Figure out iOS build verification path (no Mac on hand — likely a CI
   runner later)
4. Begin Sprint 5 — History & Settings
