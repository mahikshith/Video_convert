# Next Steps

1. Commit this session's fixes: analyzer cleanup (lint config + const fix) and Flutter
   SDK reinstall notes in context/CURRENT_STATE.md
2. Install Android SDK (command-line tools are enough) when ready, then run
   `flutter build apk --debug` to close out Sprint 1's platform-build criterion
3. Figure out iOS build verification path (no Mac on hand — likely a CI runner later)
4. Push to origin/work
5. Begin Sprint 2 — FFmpeg Engine
