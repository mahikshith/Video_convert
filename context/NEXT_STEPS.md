# Next Steps

1. Commit and push Sprint 3 (Presets UI)
2. Install Android SDK when ready, then do a real device/emulator smoke
   test covering: each compression tier, each destination preset (esp.
   Email's bitrate math), Cancel mid-encode, Save, Share
3. Figure out iOS build verification path (no Mac on hand — likely a CI
   runner later)
4. Begin Sprint 4 — GIF + Audio Extraction
